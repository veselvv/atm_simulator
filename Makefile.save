# Коментарий, говорит, что переменная CC указывает компилятор, используемый для сборки
CC=gcc

#В переменнос CFLAGS лежвт флаги, которые используются для компиляции
CFLAGS=-c -Wall

all: atm_simulator
